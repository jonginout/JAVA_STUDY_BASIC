<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 매퍼 연결 -->
<mapper namespace="com.cloud.repository.mapper.MemberMapper">

	<resultMap id="memberMap" type="member">
		<result column="member_no" property="memberNo" />
		<result column="id" property="id" />
		<result column="name" property="name" />
		<result column="email" property="email" />
		<result column="pass" property="pass" />
		<result column="address" property="address" />
		<result column="reg_date" property="regDate" />
		<result column="max_volume" property="maxVolume" />
		<result column="profile_img" property="profileImg" />
	</resultMap>
	
	<select id="idChk" parameterType="string" resultType="int">
		select
			count(id)
		from members
			where id = #{id}
	</select>
	
	<insert id="memberInsert" parameterType="member">
		insert into members
			(id, pass, name, email, reg_date, address, profile_img)
		values
			(#{id}, #{pass}, #{name}, #{email}, now(), #{address}, #{profileImg})
	</insert>

	<select id="memberLogin" parameterType="member" resultMap="memberMap">
		select *
			from members
		where id = #{id} and pass = #{pass}
	</select>
	
</mapper>




