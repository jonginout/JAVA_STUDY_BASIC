doctype html
html
  head
    meta(charset='UTF-8')
    title 노드 게시판
  body
    h1 노드 게시판
    table(style='border: 1px solid black')
      tr
        th 제목
        td #{board.title}
      tr
        th 글쓴이
        td #{board.writer}
      tr
        th 내용
        td #{board.content}
      tr
        td(colspan='2')
          a(href='/board/updateform/'+board.no) 수정
          a(href='/board/delete/'+board.no) 삭제
          a(href='/board/list') 목록
